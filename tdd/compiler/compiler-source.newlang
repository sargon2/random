
outfile = args[2];
infile = args[3];

make_assembly = (infile) {
    # TODO
};

write_tempfile = (contents) {
    # TODO: This function should be in a central library somewhere...
    # TODO
};

write_executable = (assembly, outfile) {
    # Write to file
    assembly_file = write_tempfile(assembly);

    # Invoke gcc to assemble
    # `gcc -o {outfile} {assembly_file}`;
};

assembly = make_assembly(infile);
write_executable(assembly, outfile);
