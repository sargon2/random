#!/usr/bin/python
header = '#!/usr/bin/python\nheader = '
footer = '\nprint header + repr(header) + "\\nfooter = " + repr(footer) + footer'
print header + repr(header) + "\nfooter = " + repr(footer) + footer
